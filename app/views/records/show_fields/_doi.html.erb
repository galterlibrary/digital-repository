<% Array(record[key]).reject(&:blank?).each do |v| %>
  <span itemprop="DOI" itemscope itemtype="http://schema.org/Thing">
    <%= link_to v.gsub('doi:', ''), "https://doi.org/#{v.gsub('doi:', '')}" %>
  </span>
  <br />
<% end %>
