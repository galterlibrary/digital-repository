<% if f.object.class.multiple? key %>
  <%= f.input :private_note, as: :multi_value_with_help, input_html: { rows: '9', type: 'textarea'}, required: false %>
<% else %>
  <%= f.input :private_note, as: :single_text_with_help, input_html: { rows: '9' } %>
<% end %>
